# Anthropic API Key (optional — if set, users don't need to provide their own)
# In hosted mode, set this in your Netlify environment variables.
# In BYO key mode, users enter their key in the UI settings panel.
ANTHROPIC_API_KEY=
